@page
@model LogoutModel
@{
    ViewData["Title"] = "Выход из учетной записи";
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">Нажмите, чтобы выйти из акаунта</button>
            </form>
        }
        else
        {
            <p>Вы успешнно вышли их учетной записи.</p>
        }
    }
</header>
